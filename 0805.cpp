#include <iostream>
#include <math.h>
using namespace std;class CharArray{    public:      CharArray(int l)//构造函数      {            Length=l;          Buff=new char[Length];      }        ~CharArray()//析构函数      { delete Buff;}      int GetLength()//获得长度      { return Length;}      char & operator [](int i);    private:      int Length;      char *Buff;};char & CharArra::operator [](int i)//重载{    static char ch=0;    if(i<Length&&i>=0)//检查i的范围，i=6时，不能再小于lentgh=6，所以出错。      return Buff[i];//返回数值本身，否则显示越界，buff[0]=string2[0]    else    {      cout<<"\nIndex out of range.";      return ch;//返回初值0，函数中其实没有用到。    }}void main(){    int cnt;    CharArray string1(6);    char *string2="string";//长度6个字符    for(cnt=0;cnt<8;cnt++)//从0循环到7，共8次       string1[cnt]=string2[cnt];//把string2的值赋给string1    cout<<"\n";    for(cnt=0;cnt<8;cnt++)       cout<<string1[cnt];//输出第三行数据，因为要循环8次，所以输出string6个字符后，报2次错误    cout<<"\n";    cout<<string1.GetLength()<<endl;//返回长度}/*输出结果Index out of range.//第7次越界Index out of range.//第8次越界stringIndex out of range.Index out of range.6
*/